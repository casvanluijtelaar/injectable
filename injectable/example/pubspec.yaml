name: example
description: A simple command-line application.

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  get_it:  ">=7.0.0 <8.0.0"
  injectable:
    git:
      url: git://github.com/casvanluijtelaar/injectable.git  
      path: injectable

dev_dependencies:
  pedantic: ^1.11.1
  build_runner: ^2.1.1
  effective_dart: ^1.3.2
  injectable_generator:
    git:
      url: git://github.com/casvanluijtelaar/injectable.git  
      path: injectable_generator
  injectable_generator:
    path: ../../injectable_generator
